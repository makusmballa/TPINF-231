#include <stdio.h>#define MAX 10/* Dossier AlgorithmiqueObjectifPermettre à l’utilisateur de :- Saisir une matrice- Choisir un type de vecteur (ligne ou colonne)- Saisir les éléments du vecteur- Multiplier la matrice avec le vecteur selon le type choisi- Afficher le résultatDonnées Structure Matricetypedef struct {    int lignes;               // Nombre de lignes    int colonnes;             // Nombre de colonnes    int data[MAX][MAX];       // Tableau 2D des éléments} Matrice; Variables- matrice : la matrice principale- vecteur : le vecteur choisi (ligne ou colonne)- resultat : résultat de la multiplication- choix : type de vecteur (1 = ligne, 2 = colonne)Sous-programmes1.  LireMatrice(Matrice *M)- Saisie des dimensions et des éléments d’une matrice- Utilise deux boucles imbriquées pour remplir data[i][j]2.  lireVecteur(Matrice *V, int type, int taille)- Préconfigure les dimensions selon le type :  - Ligne : lignes = 1, colonnes = taille  - Colonne : lignes = taille, colonnes = 1- Saisie des éléments du vecteur3.  afficherMatrice(Matrice M)- Affiche les éléments ligne par ligne4.  produitVecteurLigneMatrice(Matrice vecteur, Matrice matrice)- Vérifie compatibilité : vecteur.colonnes == matrice.lignes- Calcule le produit : vecteur ligne × matrice- Résultat : vecteur ligne5.  produitMatriceVecteurColonne(Matrice matrice, Matrice vecteur)- Vérifie compatibilité : matrice.colonnes == vecteur.lignes- Calcule le produit : matrice × vecteur colonne- Résultat : vecteur colonneAlgorithme principal (main)Début    Afficher "=== MATRICE ==="    Appeler lireMatrice(&matrice)    Afficher "Choisissez le type de vecteur"    Lire choix    Si choix == 1 alors        Appeler lireVecteur(&vecteur, 1, matrice.lignes)        Appeler produitVecteurLigneMatrice(vecteur, matrice) → resultat    Sinon si choix == 2 alors        Appeler lireVecteur(&vecteur, 2, matrice.colonnes)        Appeler produitMatriceVecteurColonne(matrice, vecteur) → resultat    Sinon        Afficher "Choix invalide"Fin*/typedef struct {    int lignes;    int colonnes;    int data[MAX][MAX];} Matrice;// Lecture d'une matricevoid lireMatrice(Matrice *M) {    printf("Entrez le nombre de lignes : ");    scanf("%d", &M->lignes);    printf("Entrez le nombre de colonnes : ");    scanf("%d", &M->colonnes);    printf("Entrez les éléments :\n");    for(int i = 0; i < M->lignes; i++) {        for(int j = 0; j < M->colonnes; j++) {            printf("[%d][%d] = ", i, j);            scanf("%d", &M->data[i][j]);        }    }}// Lecture d’un vecteur préconfigurévoid lireVecteur(Matrice *V, int type, int taille) {    if(type == 1) { // vecteur ligne        V->lignes = 1;        V->colonnes = taille;    } else { // vecteur colonne        V->lignes = taille;        V->colonnes = 1;    }    printf("Entrez les %d éléments du vecteur %s :\n", taille, (type == 1 ? "ligne" : "colonne"));    for(int i = 0; i < V->lignes; i++) {        for(int j = 0; j < V->colonnes; j++) {            printf("[%d][%d] = ", i, j);            scanf("%d", &V->data[i][j]);        }    }}void afficherMatrice(Matrice M) {    for(int i = 0; i < M.lignes; i++) {        for(int j = 0; j < M.colonnes; j++) {            printf("%d\t", M.data[i][j]);        }        printf("\n");    }}// Produit : vecteur ligne × matriceMatrice produitVecteurLigneMatrice(Matrice vecteur, Matrice matrice) {    Matrice resultat;    if(vecteur.lignes != 1 || vecteur.colonnes != matrice.lignes) {        printf("Erreur : dimensions incompatibles pour vecteur ligne × matrice !\n");        resultat.lignes = 0;        resultat.colonnes = 0;        return resultat;    }    resultat.lignes = 1;    resultat.colonnes = matrice.colonnes;    for(int j = 0; j < matrice.colonnes; j++) {        resultat.data[0][j] = 0;        for(int i = 0; i < matrice.lignes; i++) {            resultat.data[0][j] += vecteur.data[0][i] * matrice.data[i][j];        }    }    return resultat;}// Produit : matrice × vecteur colonneMatrice produitMatriceVecteurColonne(Matrice matrice, Matrice vecteur) {    Matrice resultat;    if(vecteur.colonnes != 1 || matrice.colonnes != vecteur.lignes) {        printf("Erreur : dimensions incompatibles pour matrice × vecteur colonne !\n");        resultat.lignes = 0;        resultat.colonnes = 0;        return resultat;    }    resultat.lignes = matrice.lignes;    resultat.colonnes = 1;    for(int i = 0; i < matrice.lignes; i++) {        resultat.data[i][0] = 0;        for(int j = 0; j < matrice.colonnes; j++) {            resultat.data[i][0] += matrice.data[i][j] * vecteur.data[j][0];        }    }    return resultat;}int main() {    Matrice vecteur, matrice, resultat;    int choix;    printf("=== MATRICE ===\n");    lireMatrice(&matrice);    printf("\nChoisissez le type de vecteur :\n");    printf("1. Vecteur ligne (1 × n)\n");    printf("2. Vecteur colonne (n × 1)\n");    printf("Votre choix : ");    scanf("%d", &choix);    if(choix == 1) {        lireVecteur(&vecteur, 1, matrice.lignes);        resultat = produitVecteurLigneMatrice(vecteur, matrice);    } else if(choix == 2) {        lireVecteur(&vecteur, 2, matrice.colonnes);        resultat = produitMatriceVecteurColonne(matrice, vecteur);    } else {        printf("Choix invalide.\n");        return 1;    }    if(resultat.lignes != 0) {        printf("\nRésultat du produit :\n");        afficherMatrice(resultat);    }    return 0;}
